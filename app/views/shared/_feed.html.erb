<% unless @feed_items.empty? %>
<table class="microposts zebra-striped" summary="User microposts">
	<%= render :partial => 'shared/feed_item', :collection => @feed_items %>
</table>
<div class="pages">
	<div class=" digg_pagination">
		<%= will_paginate @feed_items %>
	</div>
</div>

<% end %> 