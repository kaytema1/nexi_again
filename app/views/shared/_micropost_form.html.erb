<%= form_for @micropost do |f| %>
<%= render 'shared/error_messages', :object => f.object %>
<div class="field">
	<%= f.text_area :content, :rows => '60' %>
</div>
<%= f.hidden_field  :user_id, :value => current_user.id %>

<%= f.submit "Submit", :class => "large btn success"%>

<% end %> 